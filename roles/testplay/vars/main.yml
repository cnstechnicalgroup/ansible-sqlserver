---
# sa_password: "{{ lookup('credstash', 'SA') }}" AWS credstash
sa_password: "{{ lookup('pipe', 'pass MSSQL/{{inventory_hostname}}/SA') }}"
availability_group: "ag1"
primary_replica: "db1"
mirror_replica: "db2"
